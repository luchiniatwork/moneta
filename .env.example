# Required: OpenAI API key for generating embeddings (API server)
OPENAI_API_KEY=sk-your-openai-api-key

# Required: Project identifier (sent by clients via X-Project-Id header)
MONETA_PROJECT_ID=my-project

# Optional: API authentication key (if unset, auth is disabled)
# MONETA_API_KEY=
