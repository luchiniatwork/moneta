# Required: OpenAI API key for generating embeddings
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: Project identifier (default: my-project)
# MONETA_PROJECT_ID=my-project

# Optional: API authentication key (if unset, auth is disabled)
# MONETA_API_KEY=
